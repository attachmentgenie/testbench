--- /dev/null
+++ /dev/null
@@ -29,14 +29,14 @@
 
     /**
      * Run the check provided.
-     * 
+     *
      * @param Check $check Check to execute
-     * 
+     *
      * @return mixed
      */
-    static function check (Check $check) 
+    static function check (Check $check)
     {
 
         return $check->run();
     }
-}+}
